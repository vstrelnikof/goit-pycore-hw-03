# 📚 Персональний помічник

Консольна програма для управління контактами та нотатками з інтелектуальним розпізнаванням команд.

## 📁 Структура проекту

```
personal_assistant/
│
├── personal_assistant/           # Головний пакет
│   ├── __init__.py              # Ініціалізація пакету
│   │
│   ├── address_book/            # Модуль адресної книги
│   │   ├── __init__.py          # Експорт класів модуля
│   │   ├── models.py            # Моделі даних (Field, Name, Phone, Email, Birthday, Address, Record)
│   │   └── address_book.py      # Клас AddressBook
│   │
│   ├── notes/                   # Модуль нотаток
│   │   ├── __init__.py          # Експорт класів модуля
│   │   ├── note.py              # Клас Note
│   │   └── notebook.py          # Клас NoteBook
│   │
│   └── utils/                   # Допоміжні утиліти
│       ├── __init__.py          # Експорт утиліт
│       └── command_parser.py    # Парсер команд
│
├── data/                        # Дані програми (створюється автоматично)
│   ├── contacts.json            # JSON файл з контактами
│   └── notes.json               # JSON файл з нотатками
│
├── main.py                      # Головний файл для запуску
├── demo.py                      # Демонстрація можливостей
└── README.md                    # Документація
```

## 🎯 Функціонал

### Управління контактами
- ✅ Додавання контактів з повною інформацією (ім'я, телефон, email, адреса, день народження)
- ✅ Редагування контактів
- ✅ Видалення контактів
- ✅ Пошук контактів за різними критеріями
- ✅ Перегляд списку контактів
- ✅ Показ днів народження на найближчий період
- ✅ Валідація телефону (10 цифр) та email

### Управління нотатками
- ✅ Додавання нотаток з текстом
- ✅ Редагування нотаток
- ✅ Видалення нотаток
- ✅ Пошук нотаток за текстом
- ✅ Додавання тегів до нотаток
- ✅ Пошук нотаток за тегами
- ✅ Перегляд всіх тегів

### Додаткові можливості
- ✅ Інтелектуальне розпізнавання команд (можна вводити команди текстом)
- ✅ Автоматичне збереження даних на диск у JSON форматі
- ✅ Завантаження даних при запуску
- ✅ Зручний текстовий інтерфейс
- ✅ Модульна архітектура з чіткою структурою

## 📦 Встановлення та запуск

### Вимоги
- Python 3.7 або вище

### Запуск

1. Перейдіть у директорію проекту
2. Запустіть програму:

```bash
python main.py
```

Для демонстрації можливостей:
```bash
python demo.py
```

## 💡 Використання модулів

### Імпорт з пакету

```python
# Імпорт з головного пакету
from personal_assistant import AddressBook, Record, NoteBook, Note, CommandParser

# Імпорт з окремих модулів
from personal_assistant.address_book import AddressBook, Record
from personal_assistant.notes import NoteBook, Note
from personal_assistant.utils import CommandParser
```

### Приклад використання AddressBook

```python
from personal_assistant.address_book import AddressBook, Record

# Створення адресної книги
book = AddressBook()

# Додавання контакту
record = Record("Іван Петренко")
record.add_phone("0991234567")
record.add_email("ivan@example.com")
book.add_record(record)

# Збереження
book.save()

# Завантаження
book.load()
```

### Приклад використання NoteBook

```python
from personal_assistant.notes import NoteBook, Note

# Створення блокноту
notebook = NoteBook()

# Додавання нотатки
note = Note("Назва", "Текст нотатки")
note.add_tag("важливо")
notebook.add_note(note)

# Збереження
notebook.save()

# Завантаження
notebook.load()
```

## 💾 Зберігання даних

### Розташування файлів
Всі дані зберігаються в папці `data/` у корені проекту у **JSON форматі**:
```
data/
├── contacts.json        # Контакти
└── notes.json          # Нотатки
```

### Чому JSON?

✅ **Читабельність** - можна відкрити та редагувати у будь-якому текстовому редакторі  
✅ **Переносимість** - працює на всіх платформах без проблем  
✅ **Безпека** - легко перевірити вміст файлу перед завантаженням  
✅ **Сумісність** - легко інтегрувати з іншими програмами та інструментами  
✅ **Відлагодження** - зручно переглядати дані під час розробки

### Приклад JSON файлу контактів

```json
[
  {
    "name": "Іван Петренко",
    "phones": ["0991234567", "0931234567"],
    "email": "ivan@example.com",
    "birthday": "15.03.1990",
    "address": "вул. Шевченка, 15, Київ"
  }
]
```

### Приклад JSON файлу нотаток

```json
[
  {
    "title": "Ідеї для проекту",
    "content": "Додати експорт контактів",
    "tags": ["робота", "проект", "ідеї"],
    "created_at": "2026-02-04T14:30:00",
    "updated_at": "2026-02-04T14:30:00"
  }
]
```

## ✅ Валідація даних

### Номер телефону
- Формат: рівно 10 цифр
- Приклад: `0991234567`
- Неправильно: `099-123-45-67`, `+380991234567`

### Email
- Стандартний формат email
- Приклад: `user@example.com`
- Неправильно: `user@example`, `user.example.com`

### День народження
- Формат: `ДД.ММ.РРРР`
- Приклад: `15.03.1990`
- Неправильно: `15/03/1990`, `1990-03-15`

## 🏗️ Архітектура

### Модульна структура

#### address_book/
- **models.py** - Класи даних з валідацією (Field, Name, Phone, Email, Birthday, Address, Record)
- **address_book.py** - Клас AddressBook для управління контактами
- **__init__.py** - Експорт всіх класів модуля

#### notes/
- **note.py** - Клас Note для окремої нотатки
- **notebook.py** - Клас NoteBook для управління нотатками
- **__init__.py** - Експорт всіх класів модуля

#### utils/
- **command_parser.py** - Парсер для інтелектуального розпізнавання команд
- **__init__.py** - Експорт утиліт

### Переваги модульної архітектури

✅ **Чітка структура** - кожен модуль відповідає за свою функціональність  
✅ **Легке тестування** - можна тестувати кожен модуль окремо  
✅ **Простота розширення** - легко додавати нові можливості  
✅ **Зручність імпорту** - всі класи доступні через __init__.py  
✅ **Повторне використання** - модулі можна використовувати в інших проектах

## 📋 Чек-лист реалізованих вимог

### Основні вимоги (90 балів)
- ✅ Додавання контактів з повною інформацією
- ✅ Пошук контактів за різними критеріями
- ✅ Редагування та видалення контактів
- ✅ Виведення днів народження через задану кількість днів
- ✅ Валідація телефону та email
- ✅ Додавання текстових нотаток
- ✅ Пошук, редагування та видалення нотаток
- ✅ Збереження даних на жорсткому диску (JSON формат)
- ✅ Можливість перезапуску без втрати даних

### Додаткові вимоги (10 балів)
- ✅ Додавання тегів до нотаток
- ✅ Пошук та сортування нотаток за тегами
- ✅ Інтелектуальний аналіз команд
- ✅ Модульна архітектура з __init__.py

---

**Версія:** 2.0 (JSON Edition + Modular Structure)  
**Дата оновлення:** 9 лютого 2026
